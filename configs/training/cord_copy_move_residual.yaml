experiment_id: RESIDUAL-COPYMOVE-001
hypothesis: Higher-resolution fixed high-pass evidence improves copy-move seam localization and image ranking.
seed: 20260723
device: auto
model_name: residual_unet_joint
base_channels: 8
data:
  dataset_name: forgelens/cord-copy-move
  dataset_version: v1
  root: F:/HYPERVERGE
  split_manifest: configs/data/cord_copy_move_v1.json
  image_size: [192, 288]
  group_key: source_group
training:
  epochs: 5
  batch_size: 8
  learning_rate: 0.0005
  gradient_accumulation_steps: 1
  mixed_precision: true
  checkpoint_every_epochs: 1
